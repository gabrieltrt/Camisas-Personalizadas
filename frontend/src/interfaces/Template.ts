
// interface dos modelos
export default interface Template {
    id: number,
    name: string,
    mockup_front: string,
    mockup_back: string,
    overlay_front: string,
    overlay_back: string,
}